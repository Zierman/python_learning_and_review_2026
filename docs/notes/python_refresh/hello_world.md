# Hello World

Instead of strictly following the tutorial I want to start with a hello world script.

To start I'm going to make it simply output "Hello, World!" to standard output.

I wanted to get tests working but I forgot a lot of the venv setup stuff so I'm reviewing that.

## venv Setup

This is the suggestion given by AI
```bash
# Create environment
python -m venv .venv

# Activate it

# Install project + development dependency
python -m pip install -e .
python -m pip install pytest

# Run tests
python -m pytest
```